services:
  backend:
    profiles: ["future"]
    build:
      context: ./backend
    env_file:
      - .env
    ports:
      - "8000:8000"

  frontend:
    profiles: ["future"]
    build:
      context: ./frontend
    ports:
      - "5173:5173"
